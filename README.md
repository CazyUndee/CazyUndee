I build ML projects, like models, training scripts, whatever I find interesting. 

Currently working on TileFlow, an inference engine that tiles and streams tensors so massive models can actually run on consumer hardware.
